public class basicCalc {
    public integer add(integer x, integer y){
        integer sum = x + y;
        return sum;

    }
    public decimal average(integer a, integer b){
        //integer summation = add(a,b);
        decimal avg = add(a,b)/2;
        return avg;
    }
}
